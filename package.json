{
  "name": "spark-vanilla",
  "version": "0.9.1",
  "description": "A comprehensive interface development toolkit.",
  "repository": {
    "type": "git",
    "url": "https://github.com/pixeltrove/spark-vanilla.git"
  },
  "license": "MIT",
  "scripts": {
    "build:css": "npm run compile:css && npm run minify:css",
    "compile:css": "sass --no-source-map source/spark.scss build/styles/spark.css",
    "minify:css": "sass --no-source-map --style compressed build/styles/spark.css build/styles/spark.min.css"
  },
  "devDependencies": {
    "sass": "^1.23.0"
  }
}
